/**
 * Origen público de producción, confirmado por el dueño del proyecto
 * (proyecto Vercel conectado a `main`; el mismo dominio ya vive fijo en las
 * etiquetas Open Graph de `index.html`).
 *
 * Una sola constante para construir cualquier URL absoluta de SEO
 * (canonical, sitemap) evita que el dominio quede repetido y desincronizado
 * en varios archivos.
 */
export const SITE_ORIGIN = 'https://dadiva-gray.vercel.app'
